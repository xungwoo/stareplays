[build]
builder = "NIXPACKS"
buildCommand = "go mod download && go build -o bin/migration-job ./cmd/migration-job/main.go"

[deploy]
startCommand = "./bin/migration-job"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

