[build]
builder = "NIXPACKS"
buildCommand = "cd backend && go mod download && go build -o bin/migration-job ./cmd/migration-job/main.go"

[deploy]
startCommand = "cd backend && ./bin/migration-job"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
