[build]
builder = "NIXPACKS"
buildCommand = "go mod download && go build -o /backend/bin/migration-job /backend/cmd/migration-job/main.go"

[deploy]
startCommand = "/backend/bin/migration-job"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
