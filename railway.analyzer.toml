[build]
builder = "NIXPACKS"
buildCommand = "go mod download && go build -o bin/analyzer-job ./cmd/analyzer-job/main.go"

[deploy]
startCommand = "./bin/analyzer-job"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
