[build]
builder = "NIXPACKS"
buildCommand = "cd backend && go mod download && go build -o bin/analyzer-job ./cmd/analyzer-job/main.go"

[deploy]
startCommand = "cd backend && ./bin/analyzer-job"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
