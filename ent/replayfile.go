// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/xungwoo/stareplays/ent/game"
	"github.com/xungwoo/stareplays/ent/replayfile"
	"github.com/xungwoo/stareplays/ent/user"
)

// ReplayFile is the model entity for the ReplayFile schema.
type ReplayFile struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// SHA256 hash
	FileHash string `json:"file_hash,omitempty"`
	// Original filename
	Filename string `json:"filename,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ReplayFileQuery when eager-loading is set.
	Edges                      ReplayFileEdges `json:"edges"`
	game_replay_files          *int
	user_uploaded_replay_files *int
	selectValues               sql.SelectValues
}

// ReplayFileEdges holds the relations/edges for other nodes in the graph.
type ReplayFileEdges struct {
	// Game holds the value of the game edge.
	Game *Game `json:"game,omitempty"`
	// Uploader holds the value of the uploader edge.
	Uploader *User `json:"uploader,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// GameOrErr returns the Game value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ReplayFileEdges) GameOrErr() (*Game, error) {
	if e.Game != nil {
		return e.Game, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: game.Label}
	}
	return nil, &NotLoadedError{edge: "game"}
}

// UploaderOrErr returns the Uploader value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ReplayFileEdges) UploaderOrErr() (*User, error) {
	if e.Uploader != nil {
		return e.Uploader, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "uploader"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ReplayFile) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case replayfile.FieldID:
			values[i] = new(sql.NullInt64)
		case replayfile.FieldFileHash, replayfile.FieldFilename:
			values[i] = new(sql.NullString)
		case replayfile.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case replayfile.ForeignKeys[0]: // game_replay_files
			values[i] = new(sql.NullInt64)
		case replayfile.ForeignKeys[1]: // user_uploaded_replay_files
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ReplayFile fields.
func (_m *ReplayFile) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case replayfile.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case replayfile.FieldFileHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field file_hash", values[i])
			} else if value.Valid {
				_m.FileHash = value.String
			}
		case replayfile.FieldFilename:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field filename", values[i])
			} else if value.Valid {
				_m.Filename = value.String
			}
		case replayfile.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case replayfile.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field game_replay_files", value)
			} else if value.Valid {
				_m.game_replay_files = new(int)
				*_m.game_replay_files = int(value.Int64)
			}
		case replayfile.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_uploaded_replay_files", value)
			} else if value.Valid {
				_m.user_uploaded_replay_files = new(int)
				*_m.user_uploaded_replay_files = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ReplayFile.
// This includes values selected through modifiers, order, etc.
func (_m *ReplayFile) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryGame queries the "game" edge of the ReplayFile entity.
func (_m *ReplayFile) QueryGame() *GameQuery {
	return NewReplayFileClient(_m.config).QueryGame(_m)
}

// QueryUploader queries the "uploader" edge of the ReplayFile entity.
func (_m *ReplayFile) QueryUploader() *UserQuery {
	return NewReplayFileClient(_m.config).QueryUploader(_m)
}

// Update returns a builder for updating this ReplayFile.
// Note that you need to call ReplayFile.Unwrap() before calling this method if this ReplayFile
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ReplayFile) Update() *ReplayFileUpdateOne {
	return NewReplayFileClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ReplayFile entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ReplayFile) Unwrap() *ReplayFile {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ReplayFile is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ReplayFile) String() string {
	var builder strings.Builder
	builder.WriteString("ReplayFile(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("file_hash=")
	builder.WriteString(_m.FileHash)
	builder.WriteString(", ")
	builder.WriteString("filename=")
	builder.WriteString(_m.Filename)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ReplayFiles is a parsable slice of ReplayFile.
type ReplayFiles []*ReplayFile
