// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/xungwoo/stareplays/ent/game"
	"github.com/xungwoo/stareplays/ent/gamedetail"
	"github.com/xungwoo/stareplays/ent/schema"
)

// GameDetailCreate is the builder for creating a GameDetail entity.
type GameDetailCreate struct {
	config
	mutation *GameDetailMutation
	hooks    []Hook
}

// SetApmTimeline sets the "apm_timeline" field.
func (_c *GameDetailCreate) SetApmTimeline(v []schema.PlayerAPMTimeline) *GameDetailCreate {
	_c.mutation.SetApmTimeline(v)
	return _c
}

// SetBuildOrders sets the "build_orders" field.
func (_c *GameDetailCreate) SetBuildOrders(v []schema.PlayerBuildOrder) *GameDetailCreate {
	_c.mutation.SetBuildOrders(v)
	return _c
}

// SetCompressedBuildOrders sets the "compressed_build_orders" field.
func (_c *GameDetailCreate) SetCompressedBuildOrders(v []schema.PlayerBuildOrder) *GameDetailCreate {
	_c.mutation.SetCompressedBuildOrders(v)
	return _c
}

// SetChatMessages sets the "chat_messages" field.
func (_c *GameDetailCreate) SetChatMessages(v []schema.ChatMessage) *GameDetailCreate {
	_c.mutation.SetChatMessages(v)
	return _c
}

// SetGameID sets the "game" edge to the Game entity by ID.
func (_c *GameDetailCreate) SetGameID(id int) *GameDetailCreate {
	_c.mutation.SetGameID(id)
	return _c
}

// SetGame sets the "game" edge to the Game entity.
func (_c *GameDetailCreate) SetGame(v *Game) *GameDetailCreate {
	return _c.SetGameID(v.ID)
}

// Mutation returns the GameDetailMutation object of the builder.
func (_c *GameDetailCreate) Mutation() *GameDetailMutation {
	return _c.mutation
}

// Save creates the GameDetail in the database.
func (_c *GameDetailCreate) Save(ctx context.Context) (*GameDetail, error) {
	return withHooks(ctx, _c.sqlSave, _c.mutation, _c.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (_c *GameDetailCreate) SaveX(ctx context.Context) *GameDetail {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *GameDetailCreate) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *GameDetailCreate) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (_c *GameDetailCreate) check() error {
	if len(_c.mutation.GameIDs()) == 0 {
		return &ValidationError{Name: "game", err: errors.New(`ent: missing required edge "GameDetail.game"`)}
	}
	return nil
}

func (_c *GameDetailCreate) sqlSave(ctx context.Context) (*GameDetail, error) {
	if err := _c.check(); err != nil {
		return nil, err
	}
	_node, _spec := _c.createSpec()
	if err := sqlgraph.CreateNode(ctx, _c.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	_c.mutation.id = &_node.ID
	_c.mutation.done = true
	return _node, nil
}

func (_c *GameDetailCreate) createSpec() (*GameDetail, *sqlgraph.CreateSpec) {
	var (
		_node = &GameDetail{config: _c.config}
		_spec = sqlgraph.NewCreateSpec(gamedetail.Table, sqlgraph.NewFieldSpec(gamedetail.FieldID, field.TypeInt))
	)
	if value, ok := _c.mutation.ApmTimeline(); ok {
		_spec.SetField(gamedetail.FieldApmTimeline, field.TypeJSON, value)
		_node.ApmTimeline = value
	}
	if value, ok := _c.mutation.BuildOrders(); ok {
		_spec.SetField(gamedetail.FieldBuildOrders, field.TypeJSON, value)
		_node.BuildOrders = value
	}
	if value, ok := _c.mutation.CompressedBuildOrders(); ok {
		_spec.SetField(gamedetail.FieldCompressedBuildOrders, field.TypeJSON, value)
		_node.CompressedBuildOrders = value
	}
	if value, ok := _c.mutation.ChatMessages(); ok {
		_spec.SetField(gamedetail.FieldChatMessages, field.TypeJSON, value)
		_node.ChatMessages = value
	}
	if nodes := _c.mutation.GameIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2O,
			Inverse: true,
			Table:   gamedetail.GameTable,
			Columns: []string{gamedetail.GameColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(game.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.game_game_detail = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// GameDetailCreateBulk is the builder for creating many GameDetail entities in bulk.
type GameDetailCreateBulk struct {
	config
	err      error
	builders []*GameDetailCreate
}

// Save creates the GameDetail entities in the database.
func (_c *GameDetailCreateBulk) Save(ctx context.Context) ([]*GameDetail, error) {
	if _c.err != nil {
		return nil, _c.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(_c.builders))
	nodes := make([]*GameDetail, len(_c.builders))
	mutators := make([]Mutator, len(_c.builders))
	for i := range _c.builders {
		func(i int, root context.Context) {
			builder := _c.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*GameDetailMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, _c.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, _c.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, _c.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (_c *GameDetailCreateBulk) SaveX(ctx context.Context) []*GameDetail {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *GameDetailCreateBulk) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *GameDetailCreateBulk) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}
