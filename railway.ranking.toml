[build]
builder = "NIXPACKS"
buildCommand = "go mod download && go build -o /backend/bin/ranking-job /backend/cmd/ranking-job/main.go"

[deploy]
startCommand = "/backend/bin/ranking-job"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
