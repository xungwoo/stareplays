[build]
builder = "NIXPACKS"
buildCommand = "cd backend && go mod download && go build -o bin/ranking-job ./cmd/ranking-job/main.go"

[deploy]
startCommand = "cd backend && ./bin/ranking-job"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
